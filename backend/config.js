// config.js

export const PORT = process.env.PORT || 10000; // Uses environment variable for PORT, with fallback to 4000
export const mongoDBURL = process.env.MONGODB_URL; // MongoDB URL set as an environment variable
